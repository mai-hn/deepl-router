[project]
name = "deepl-router"
version = "0.1.0"
description = "A self-hosted DeepL / DeepLX translation router"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "flask>=3.0,<4",
  "httpx>=0.27,<1",
  "python-multipart>=0.0.9,<1",
  "uvicorn[standard]>=0.30,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8.0,<9"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
